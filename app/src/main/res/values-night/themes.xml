<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.SGDH" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryVariant">@color/brand_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/input_stroke_focus</item>
        <item name="colorAccent">@color/input_stroke_focus</item>

        <item name="android:windowBackground">@color/background_screen</item>
        <item name="android:statusBarColor">@android:color/black</item>

        <item name="colorControlActivated">@color/input_stroke_focus</item>
    </style>

    <style name="Theme.SGDH.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.SGDH.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.SGDH.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SidebarItem">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingVertical">16dp</item>
        <item name="android:paddingHorizontal">12dp</item>
        <item name="android:textColor">@color/sidebar_text</item>
        <item name="android:textSize">16sp</item>
        <item name="android:drawablePadding">16dp</item>
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:drawableTint">@color/sidebar_text_muted</item>
    </style>
</resources>